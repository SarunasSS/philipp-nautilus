apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

nameSuffix: "-1"

transformers:
  - instance-label.yaml

resources:
  - ../../../../base
  - external-secrets.yaml

patches:
  - path: logs-storage.yaml
